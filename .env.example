# PORT: TCP port the Express server listens on.
# Defaults to 3000 locally. Cloud Run and similar platforms inject this at runtime — do not
# hardcode a port number in deployment config, the server reads it from this variable.
PORT=3000
